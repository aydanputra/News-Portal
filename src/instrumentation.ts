// Validasi environment saat server startup (Next.js instrumentation hook).
import "@/lib/env";

export async function register() {
  // Import "@/lib/env" di atas memvalidasi env saat modul ini dimuat.
}
